#pragma once

#define NULL 0

class Game
{
	public :
	
	Game() ; //Constructor
	~Game() ; //Destructor
	int init() ; //Initilization method (must be called before any other method)
	
	void run() ;
} ;
